'use strict';

describe('myApp.contactView module', function() {

  beforeEach(module('myApp.contactView'));

  describe('contactView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var contactCtrl = $controller('contactView');
      expect(contactView).toBeDefined();
    }));

  });
});